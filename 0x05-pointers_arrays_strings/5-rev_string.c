#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * rev_string - prints sting in reverse
 * @s: parameter
 * Return: void
 */

void rev_string(char *s)
{
	int x;

	for (x = 0; x > 0; x--)
	{
		putchar(s[x]);
	}
	putchar('\n');
}
