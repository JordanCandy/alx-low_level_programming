#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 if successful
 */

int main(void)
{
	char firstString[] = "\"Programming is like building a multilingual puzzle";

	puts(firstString);
	return (0);
}
