#include <stdio.h>
#include "main.h"

/**
 * _islower - checks if a character is lowercase
 * @c: Parameter for _islower
 * Return: returns 1 or 0
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
